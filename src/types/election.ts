export interface Election {
    _id: string
    title: string
    election_type: string
    posts: string[]
    start: string
    end: string
    department_eligibility: string
    createdAt: string
    updatedAt: string
    __v: number
}